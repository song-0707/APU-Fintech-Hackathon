# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Environment & secrets
.env
.env.*
!.env.example

# Google Cloud service-account key referenced by GEMINI_SERVICE_ACCOUNT_FILE
apu-fintech-hackathon-dcdcea979ca9.json
*service-account*.json

# Runtime data (generated by the pipeline, not source)
backend/storage/raw/*
backend/storage/audio/*
backend/storage/transcripts/*
backend/storage/summaries/*
backend/storage/exports/*
!backend/storage/**/.gitkeep
backend/logs/*.log
backend/*.db
backend/storage/chroma/
*.db
logs/
neo4j-data/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/
MEETINGS/uploads/
MEETINGS/results/
ASK COCO/memory/

# Teammate's real working copies (moved in under MEETINGS/) — contain real
# uploaded meeting videos, real transcripts, and a populated Chroma DB with
# real meeting content. Source code (pipeline.py etc.) at MEETINGS/ root and
# MEETINGS/MEETINGS/*.py stays tracked; only the data/runtime artifacts and
# .env (already covered above) are excluded.
MEETINGS/MEETINGS/uploads/
MEETINGS/MEETINGS/results/
MEETINGS/MEETINGS/frames/
MEETINGS/ASK_COCO/memory/

# Imported standalone prototypes are retained locally for reference. The
# integrated implementation lives under backend/app and frontend/src.
ASK_COCO/
MEETINGS/ASK_COCO/
MEETINGS/MEETINGS/

# Pitch/proposal materials (slides, recordings, videos) — not source, not
# needed by anyone cloning the repo to run the app.
Preliminary/
